# Next build — v0.8

1. Replace Finance UI seed data with authenticated Supabase queries and mutations.
2. Add invoice/devis editor, line items, deposit-to-final invoice conversion and PDF preview.
3. Add Stripe Billing Portal and cancellation/upgrade/downgrade UI.
4. Add Apple StoreKit / App Store Server API adapter and entitlement reconciliation.
5. Add admin finance-rules editor with source/effective-date audit history.
6. Add reminder approval inbox and formal-notice PDF draft workflow.
7. Add bank-import reconciliation adapter (CSV first, open-banking provider later).
8. Run full npm install/build/lint and deploy preview to Vercel.
