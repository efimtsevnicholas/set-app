import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const a=fs.readFileSync(new URL('../app/components/SetApp.js',import.meta.url),'utf8'),r=fs.readFileSync(new URL('../app/api/support/route.js',import.meta.url),'utf8');test('logout',()=>{assert.ok(a.includes('.auth.signOut()'));assert.ok(a.includes('Log out'))});test('support',()=>{assert.ok(a.includes('Support / Report a bug'));assert.ok(a.includes("fetch('/api/support'"));assert.ok(r.includes('SET_SUPPORT_EMAIL'));assert.ok(r.includes('RESEND_API_KEY'))});