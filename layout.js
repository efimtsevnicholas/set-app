import './style.css';
export const metadata = { title: 'SET — Creative Production OS', description: 'Creative production workspace' };
export default function RootLayout({ children }) { return <html lang="en"><body>{children}</body></html>; }
