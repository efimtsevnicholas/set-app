# SET 1.0.1 Hotfix

Fixes two release blockers:
1. My Business now keeps an explicit editable draft and saves the profile to both the invoice-facing local profile and the cloud-backed business-profile collection.
2. Invoice/devis composer is viewport-constrained and scrollable on desktop, tablet, and phone, with the save/create action kept reachable.

Production browser smoke testing is still part of deployment.
