'use client';import {useEffect,useState} from 'react';import {createClient} from '../../lib/supabase-browser';export default function ResetPassword(){const [password,setPassword]=useState(''),[status,setStatus]=useState(''),[ready,setReady]=useState(false);useEffect(()=>{const s=createClient();s.auth.getSession().then(({data})=>setReady(Boolean(data.session)));const {data}=s.auth.onAuthStateChange((event)=>{if(event==='PASSWORD_RECOVERY'||event==='SIGNED_IN')setReady(true)});return()=>data.subscription.unsubscribe()},[]);async function save(e){e.preventDefault();try{const s=createClient(),{error}=await s.auth.updateUser({password});if(error)throw error;setStatus('PASSWORD UPDATED. REDIRECTING…');setTimeout(()=>location.href='/',700)}catch(e){setStatus(String(e.message||'Could not update password').toUpperCase())}}return <main className="loginPage"><header className="loginHeader"><a className="loginLogo" href="/">SET</a><span>ACCOUNT SECURITY</span></header><section className="loginGrid"><div className="loginIntro"><div className="loginKicker">SET / SECURITY</div><h1>NEW PASSWORD.</h1></div><div className="loginPanel"><div className="loginPanelTop"><span>RESET PASSWORD</span><span>01</span></div>{ready?<form onSubmit={save}><label>NEW PASSWORD<input type="password" minLength="8" required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="loginSubmit">SAVE PASSWORD →</button></form>:<p className="loginStatus">OPEN THE PASSWORD RESET LINK FROM YOUR EMAIL.</p>}{status&&<div className="loginStatus">{status}</div>}</div></section></main>