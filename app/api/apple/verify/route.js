import {NextResponse} from 'next/server';import {requireUser} from '../../../../lib/server/auth.js';
export async function POST(req){try{await requireUser();const body=await req.json();if(!body?.signedTransactionInfo)return NextResponse.json({error:'signedTransactionInfo is required'},{status:400});return NextResponse.json({accepted:true,status:'verification_pending',message:'Server endpoint ready for App Store Server API verification. Configure Apple issuer ID, key ID, bundle ID and private key before production.'},{status:202})}catch(e){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORIZED'?401:500})}}
