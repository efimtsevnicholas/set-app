import {NextResponse} from 'next/server';import {requireUser} from '../../../../lib/server/auth.js';import {getStripe} from '../../../../lib/server/stripe.js';import {createSupabaseAdmin} from '../../../../lib/server/supabase-admin.js';
export async function POST(){try{const u=await requireUser(),db=createSupabaseAdmin();const {data:s}=await db.from('subscriptions').select('provider_customer_id').eq('user_id',u.id).eq('provider','stripe').not('provider_customer_id','is',null).maybeSingle();if(!s?.provider_customer_id)return NextResponse.json({error:'No Stripe customer found'},{status:404});const session=await getStripe().billingPortal.sessions.create({customer:s.provider_customer_id,return_url:`${process.env.NEXT_PUBLIC_APP_URL}/?billing=portal`});return NextResponse.json({url:session.url})}catch(e){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORIZED'?401:500})}}
