import {notFound} from 'next/navigation';import {createClient} from '@supabase/supabase-js';export const dynamic='force-dynamic';export default async function SharedCallSheet({params}){const {token}=await params;const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});const {data}=await db.rpc('get_shared_call_sheet',{p_token:token});if(!data?.project)notFound();const p=data.project,events=data.events||[],cast=data.casting||[],team=data.team||[];return <main className="sharedSheet"><div className="eyebrow">SET / SHARED CALL SHEET</div><h1>{p.name||'CALL SHEET'}</h1><section><h3>LOCATION</h3><p>{p.address||p.location||'TBD'}</p>{p.location_notes&&<p>{p.location_notes}</p>}{p.parking_info&&<p>PARKING — {p.parking_info}</p>}{p.transit_info&&<p>TRANSIT — {p.transit_info}</p>}{p.directions_url&&<p><a href={p.directions_url}>OPEN DIRECTIONS ↗</a></p>}</section><section><h3>SCHEDULE</h3>{events.map(x=><p key={x.id}>{x.start_at?new Date(x.start_at).toLocaleString():'TBD'} — {x.title}</p>)}</section><section><h3>CAST</h3>{cast.map(x=><p key={x.id}>{x.name}{x.role?' — '+x.role:''}</p>)}</section><section><h3>TEAM</h3>{team.map(x=><p key={x.id}>{x.name||x.email||'Team member'}{x.role?' — '+x.role:''}</p>)}</section><footer>SET · CREATIVE PRODUCTION OS</footer></main>}